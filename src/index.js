/**
 * Accepts a FLEx XML export (FlexText format) as a String and converts it to an Array of DLx Text Objects
 * @param  {String} xml The FLEx XML export to convert
 * @return {Array}
 */
function flextext2dlx(xml) {
  return [];
}

module.exports = flextext2dlx;
